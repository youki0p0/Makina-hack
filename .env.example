# Supabase (optional). Set these in Vercel → Environment Variables.
# If unset, the app falls back to local dummy ranking data and still works.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
